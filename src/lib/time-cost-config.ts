/** Configurable limits for time and cost business rules. */
export const DAILY_HOUR_LIMIT = 8;

/** Cost date older than this many days is flagged as a late entry. */
export const LATE_ENTRY_DAYS = 7;

/** Direct costs at or above this amount require manager approval. */
export const LARGE_COST_THRESHOLD = 10000;
